import 'package:flutter/material.dart';

class Anish extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Anish Profile'),
      ),
      body: Text('This is my profile'),
    );
  }
}